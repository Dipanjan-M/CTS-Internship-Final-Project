import { IsResolvedPipe } from './is-resolved.pipe';

describe('IsResolvedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsResolvedPipe();
    expect(pipe).toBeTruthy();
  });
});
